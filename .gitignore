# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Virtualenv / uv
.venv/
.uv/

# OS
.DS_Store

# Upstream clone (server clones this; never commit it)
diagnosis/external/

# Datasets, caches, checkpoints (heavy / generated)
diagnosis/data/metaworld/
diagnosis/data/Metaworld/
diagnosis/data/hf_mw/
diagnosis/data/droid_subset/
diagnosis/data/precomputed_latents/
diagnosis/data/robocasa/
diagnosis/models/checkpoints/
# torch.hub / HF checkpoint cache (~7.5 GB, model weights)
diagnosis/.hf_home/
diagnosis/results/.pipeline_done

# Run logs + secrets (NEVER commit the HF token)
diagnosis/logs/
**/.env

# Diagnostic outputs (regenerated by the pipeline)
diagnosis/results/*.csv
diagnosis/results/*.md
diagnosis/results/figures/*.pdf
diagnosis/results/figures/*.png
!diagnosis/results/figures/.gitkeep
